// const SimilarItemsDrawer = (() => {
//   // Singleton state - nothing initialized until first use
//   let state = {
//     drawer: null,
//     overlay: null,
//     cache: null,
//     isOpen: false,
//     abortController: null,
//     lastTriggerButton: null,
//     initialized: false
//   };

//   // Lazy-bound handlers (created only once, on first use)
//   let handlers = {};

//   // Create drawer DOM elements (called only on first button click)
//   function createDrawer() {
//     if (state.drawer) return;

//     const overlay = document.createElement("div");
//     overlay.className = "similar-items-overlay";
//     overlay.setAttribute("aria-hidden", "true");

//     const drawer = document.createElement("div");
//     drawer.className = "similar-items-drawer";
//     drawer.setAttribute("role", "dialog");
//     drawer.setAttribute("aria-modal", "true");
//     drawer.setAttribute("aria-labelledby", "similar-items-title");

//     document.body.appendChild(overlay);
//     document.body.appendChild(drawer);

//     state.overlay = overlay;
//     state.drawer = drawer;

//     attachDrawerListeners();
//   }

//   // Attach listeners only after drawer is created
//   function attachDrawerListeners() {
//     if (!handlers.overlayClick) {
//       handlers.overlayClick = () => close();
//       handlers.escapeKey = (e) => {
//         if (e.key === "Escape" && state.isOpen) close();
//       };
//       handlers.touchMove = (e) => {
//         if (state.isOpen) {
//           const content = state.drawer.querySelector(".similar-items-drawer__content");
//           if (content && !content.contains(e.target)) {
//             e.preventDefault();
//           }
//         }
//       };
//     }

//     state.overlay.addEventListener("click", handlers.overlayClick);
//     document.addEventListener("keydown", handlers.escapeKey);
//     state.drawer.addEventListener("touchmove", handlers.touchMove, { passive: false });
//   }

//   // Main click handler (delegated from document)
//   function handleClick(event) {
//     const button = event.target.closest(".view-similar-btn");

//     if (button) {
//       event.preventDefault();
//       state.lastTriggerButton = button;

//       // Create drawer on first use
//       if (!state.drawer) {
//         createDrawer();
//       }

//       const productHandle = button.dataset.productHandle;
//       if (productHandle) {
//         loadAndShow(productHandle);
//       } else {
//         console.error("Product handle not found");
//       }
//       return;
//     }

//     // Handle close button
//     if (state.isOpen && event.target.closest(".similar-items-drawer__close")) {
//       close();
//     }
//   }

//   // Show loader
//   function showLoader() {
//     state.drawer.innerHTML = `
//       <div class="similar-items-drawer__inner">
//         <div class="similar-items-drawer__loader">
//           <div class="spinner"></div>
//           <p>Loading similar items...</p>
//         </div>
//       </div>
//     `;
//   }

//   // Load and show content
//   async function loadAndShow(productHandle) {
//     showLoader();
//     open();

//     // Lazy init cache
//     if (!state.cache) {
//       state.cache = new Map();
//     }

//     // Return cached content
//     if (state.cache.has(productHandle)) {
//       renderContent(state.cache.get(productHandle));
//       return;
//     }

//     // Abort pending requests
//     if (state.abortController) {
//       state.abortController.abort();
//     }

//     state.abortController = new AbortController();

//     try {
//       const response = await fetch(`/products/${productHandle}?view=similar-items`, {
//         signal: state.abortController.signal,
//         headers: { "X-Requested-With": "XMLHttpRequest" }
//       });

//       if (!response.ok) throw new Error(`HTTP ${response.status}`);

//       const html = await response.text();

//       // Cache with size limit (LRU-style)
//       state.cache.set(productHandle, html);
//       if (state.cache.size > 10) {
//         const firstKey = state.cache.keys().next().value;
//         state.cache.delete(firstKey);
//       }

//       renderContent(html);

//     } catch (error) {
//       if (error.name !== "AbortError") {
//         console.error("Error loading similar items:", error);
//         renderError();
//       }
//     } finally {
//       state.abortController = null;
//     }
//   }

//   // Render content
//   function renderContent(html) {
//     state.drawer.innerHTML = `<div class="similar-items-drawer__inner">${html}</div>`;
    
//     // Dispatch event for third-party scripts
//     document.dispatchEvent(new CustomEvent("similar-items:loaded", {
//       detail: { drawer: state.drawer }
//     }));
//   }

//   // Render error
//   function renderError() {
//     state.drawer.innerHTML = `
//       <div class="similar-items-drawer__inner">
//         <div class="similar-items-drawer__error">
//           <p>Unable to load similar items. Please try again.</p>
//           <button type="button" class="similar-items-drawer__close btn">Close</button>
//         </div>
//       </div>
//     `;
//   }

//   // Open drawer
//   function open() {
//     state.isOpen = true;
//     document.body.style.overflow = "hidden";

//     requestAnimationFrame(() => {
//       state.overlay.classList.add("is-visible");
//       state.drawer.classList.add("is-open");
//       state.overlay.setAttribute("aria-hidden", "false");
//       trapFocus();
//     });
//   }

//   // Close drawer
//   function close() {
//     if (!state.isOpen) return;

//     state.isOpen = false;
//     document.body.style.overflow = "";
//     state.overlay.classList.remove("is-visible");
//     state.drawer.classList.remove("is-open");
//     state.overlay.setAttribute("aria-hidden", "true");

//     returnFocus();
//   }

//   // Focus trap
//   function trapFocus() {
//     const focusable = state.drawer.querySelectorAll(
//       'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
//     );

//     if (focusable.length === 0) return;

//     const first = focusable[0];
//     const last = focusable[focusable.length - 1];

//     requestAnimationFrame(() => first?.focus());

//     if (!handlers.focusTrap) {
//       handlers.focusTrap = (e) => {
//         if (e.key !== "Tab") return;

//         const focusable = state.drawer.querySelectorAll(
//           'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
//         );

//         if (focusable.length === 0) return;

//         const first = focusable[0];
//         const last = focusable[focusable.length - 1];

//         if (e.shiftKey && document.activeElement === first) {
//           last.focus();
//           e.preventDefault();
//         } else if (!e.shiftKey && document.activeElement === last) {
//           first.focus();
//           e.preventDefault();
//         }
//       };
//     }

//     state.drawer.addEventListener("keydown", handlers.focusTrap);
//   }

//   // Return focus
//   function returnFocus() {
//     if (handlers.focusTrap) {
//       state.drawer.removeEventListener("keydown", handlers.focusTrap);
//     }

//     state.lastTriggerButton?.focus();
//   }

//   // Clear cache
//   function clearCache() {
//     state.cache?.clear();
//   }

//   // Destroy instance
//   function destroy() {
//     document.removeEventListener("click", handleClick);
    
//     if (state.overlay) {
//       state.overlay.removeEventListener("click", handlers.overlayClick);
//       state.overlay.remove();
//     }

//     if (state.drawer) {
//       state.drawer.remove();
//     }

//     state.abortController?.abort();
//     clearCache();
    
//     // Reset state
//     state = {
//       drawer: null,
//       overlay: null,
//       cache: null,
//       isOpen: false,
//       abortController: null,
//       lastTriggerButton: null,
//       initialized: false
//     };
//   }

//   // Initialize (attach single document listener)
//   function init() {
//     if (state.initialized) return;
//     document.addEventListener("click", handleClick);
//     state.initialized = true;
//   }

//   // Public API
//   return {
//     init,
//     clearCache,
//     destroy,
//     get isOpen() { return state.isOpen; }
//   };
// })();

// // Auto-initialize with minimal footprint
// if (document.readyState === "loading") {
//   document.addEventListener("DOMContentLoaded", () => SimilarItemsDrawer.init(), { once: true });
// } else {
//   SimilarItemsDrawer.init();
// }

// // Expose globally
// window.similarItemsDrawer = SimilarItemsDrawer;

const SimilarItemsDrawer = (() => {
  // Singleton state - nothing initialized until first use
  let state = {
    drawer: null,
    overlay: null,
    cache: null,
    isOpen: false,
    abortController: null,
    lastTriggerButton: null,
    initialized: false
  };

  // Lazy-bound handlers (created only once, on first use)
  let handlers = {};

  // Helper function to detect page type
  function getPageType() {
    const path = window.location.pathname;
    const bodyClasses = document.body.className;

    // Check for Shopify template classes
    if (bodyClasses.includes('template-index') || path === '/') {
      return 'homepage';
    } else if (bodyClasses.includes('template-collection') || path.includes('/collections/')) {
      return 'plp';
    } else if (bodyClasses.includes('template-product') || path.includes('/products/')) {
      return 'pdp';
    } else if (bodyClasses.includes('template-search') || path.includes('/search')) {
      return 'search';
    }

    // Fallback: check URL structure
    if (path === '/' || path === '/index') return 'homepage';
    if (path.match(/^\/collections\//)) return 'plp';
    if (path.match(/^\/products\//)) return 'pdp';

    return 'other';
  }

  // Helper function to extract product name from button
  function getProductName(button) {
    // Try to get from aria-label first
    const ariaLabel = button.getAttribute('aria-label');
    if (ariaLabel) {
      const match = ariaLabel.match(/View similar items for (.+)/i);
      if (match && match[1]) {
        return match[1];
      }
    }

    // Try to get from data attribute if added
    if (button.dataset.productTitle) {
      return button.dataset.productTitle;
    }

    // Try to find product title in parent card
    const productCard = button.closest('.card, .product-card, [class*="product-"]');
    if (productCard) {
      const titleElement = productCard.querySelector('.card__title, .product-card__title, [class*="product-title"], [class*="card-title"]');
      if (titleElement) {
        return titleElement.textContent.trim();
      }
    }

    // Fallback to product handle if nothing else works
    return button.dataset.productHandle || 'Unknown Product';
  }

  // Push to dataLayer
  function trackPromoClick(button) {
    // Ensure dataLayer exists
    window.dataLayer = window.dataLayer || [];

    const pageType = getPageType();
    const productName = getProductName(button);

    window.dataLayer.push({
      event: "promoClick",
      promoClick: {
        creative_name: "View Similar Button",
        promo_name: productName,
        location_id: pageType
      }
    });

    // Optional: Log for debugging
    console.log('DataLayer pushed:', {
      event: "promoClick",
      page_type: pageType,
      product_name: productName
    });
  }

  // Create drawer DOM elements (called only on first button click)
  function createDrawer() {
    if (state.drawer) return;

    const overlay = document.createElement("div");
    overlay.className = "similar-items-overlay";
    overlay.setAttribute("aria-hidden", "true");

    const drawer = document.createElement("div");
    drawer.className = "similar-items-drawer";
    drawer.setAttribute("role", "dialog");
    drawer.setAttribute("aria-modal", "true");
    drawer.setAttribute("aria-labelledby", "similar-items-title");

    document.body.appendChild(overlay);
    document.body.appendChild(drawer);

    state.overlay = overlay;
    state.drawer = drawer;

    attachDrawerListeners();
  }

  // Attach listeners only after drawer is created
  function attachDrawerListeners() {
    if (!handlers.overlayClick) {
      handlers.overlayClick = () => close();
      handlers.escapeKey = (e) => {
        if (e.key === "Escape" && state.isOpen) close();
      };
      handlers.touchMove = (e) => {
        if (state.isOpen) {
          const content = state.drawer.querySelector(".similar-items-drawer__content");
          if (content && !content.contains(e.target)) {
            e.preventDefault();
          }
        }
      };
    }

    state.overlay.addEventListener("click", handlers.overlayClick);
    document.addEventListener("keydown", handlers.escapeKey);
    state.drawer.addEventListener("touchmove", handlers.touchMove, { passive: false });
  }

  // Main click handler (delegated from document)
  function handleClick(event) {
    const button = event.target.closest(".view-similar-btn");

    if (button) {
      event.preventDefault();
      state.lastTriggerButton = button;

      // Track the click in dataLayer
      trackPromoClick(button);

      // Create drawer on first use
      if (!state.drawer) {
        createDrawer();
      }

      const productHandle = button.dataset.productHandle;
      if (productHandle) {
        loadAndShow(productHandle);
      } else {
        console.error("Product handle not found");
      }
      return;
    }

    // Handle close button
    if (state.isOpen && event.target.closest(".similar-items-drawer__close")) {
      close();
    }
  }

  // Show loader
  function showLoader() {
    state.drawer.innerHTML = `
      <div class="similar-items-drawer__inner">
        <div class="similar-items-drawer__loader">
          <div class="spinner"></div>
          <p>Loading similar items...</p>
        </div>
      </div>
    `;
  }

  // Load and show content
  async function loadAndShow(productHandle) {
    showLoader();
    open();

    // Lazy init cache
    if (!state.cache) {
      state.cache = new Map();
    }

    // Return cached content
    if (state.cache.has(productHandle)) {
      renderContent(state.cache.get(productHandle));
      return;
    }

    // Abort pending requests
    if (state.abortController) {
      state.abortController.abort();
    }

    state.abortController = new AbortController();

    try {
      const response = await fetch(`/products/${productHandle}?view=similar-items`, {
        signal: state.abortController.signal,
        headers: { "X-Requested-With": "XMLHttpRequest" }
      });

      if (!response.ok) throw new Error(`HTTP ${response.status}`);

      const html = await response.text();

      // Cache with size limit (LRU-style)
      state.cache.set(productHandle, html);
      if (state.cache.size > 10) {
        const firstKey = state.cache.keys().next().value;
        state.cache.delete(firstKey);
      }

      renderContent(html);

    } catch (error) {
      if (error.name !== "AbortError") {
        console.error("Error loading similar items:", error);
        renderError();
      }
    } finally {
      state.abortController = null;
    }
  }

  // Render content
  function renderContent(html) {
    state.drawer.innerHTML = `<div class="similar-items-drawer__inner">${html}</div>`;
    
    // Dispatch event for third-party scripts
    document.dispatchEvent(new CustomEvent("similar-items:loaded", {
      detail: { drawer: state.drawer }
    }));
  }

  // Render error
  function renderError() {
    state.drawer.innerHTML = `
      <div class="similar-items-drawer__inner">
        <div class="similar-items-drawer__error">
          <p>Unable to load similar items. Please try again.</p>
          <button type="button" class="similar-items-drawer__close btn">Close</button>
        </div>
      </div>
    `;
  }

  // Open drawer
  function open() {
    state.isOpen = true;
    document.body.style.overflow = "hidden";

    requestAnimationFrame(() => {
      state.overlay.classList.add("is-visible");
      state.drawer.classList.add("is-open");
      state.overlay.setAttribute("aria-hidden", "false");
      trapFocus();
    });
  }

  // Close drawer
  function close() {
    if (!state.isOpen) return;

    state.isOpen = false;
    document.body.style.overflow = "";
    state.overlay.classList.remove("is-visible");
    state.drawer.classList.remove("is-open");
    state.overlay.setAttribute("aria-hidden", "true");

    returnFocus();
  }

  // Focus trap
  function trapFocus() {
    const focusable = state.drawer.querySelectorAll(
      'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
    );

    if (focusable.length === 0) return;

    const first = focusable[0];
    const last = focusable[focusable.length - 1];

    requestAnimationFrame(() => first?.focus());

    if (!handlers.focusTrap) {
      handlers.focusTrap = (e) => {
        if (e.key !== "Tab") return;

        const focusable = state.drawer.querySelectorAll(
          'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
        );

        if (focusable.length === 0) return;

        const first = focusable[0];
        const last = focusable[focusable.length - 1];

        if (e.shiftKey && document.activeElement === first) {
          last.focus();
          e.preventDefault();
        } else if (!e.shiftKey && document.activeElement === last) {
          first.focus();
          e.preventDefault();
        }
      };
    }

    state.drawer.addEventListener("keydown", handlers.focusTrap);
  }

  // Return focus
  function returnFocus() {
    if (handlers.focusTrap) {
      state.drawer.removeEventListener("keydown", handlers.focusTrap);
    }

    state.lastTriggerButton?.focus();
  }

  // Clear cache
  function clearCache() {
    state.cache?.clear();
  }

  // Destroy instance
  function destroy() {
    document.removeEventListener("click", handleClick);
    
    if (state.overlay) {
      state.overlay.removeEventListener("click", handlers.overlayClick);
      state.overlay.remove();
    }

    if (state.drawer) {
      state.drawer.remove();
    }

    state.abortController?.abort();
    clearCache();
    
    // Reset state
    state = {
      drawer: null,
      overlay: null,
      cache: null,
      isOpen: false,
      abortController: null,
      lastTriggerButton: null,
      initialized: false
    };
  }

  // Initialize (attach single document listener)
  function init() {
    if (state.initialized) return;
    document.addEventListener("click", handleClick);
    state.initialized = true;
  }

  // Public API
  return {
    init,
    clearCache,
    destroy,
    get isOpen() { return state.isOpen; }
  };
})();

// Auto-initialize with minimal footprint
if (document.readyState === "loading") {
  document.addEventListener("DOMContentLoaded", () => SimilarItemsDrawer.init(), { once: true });
} else {
  SimilarItemsDrawer.init();
}

// Expose globally
window.similarItemsDrawer = SimilarItemsDrawer;