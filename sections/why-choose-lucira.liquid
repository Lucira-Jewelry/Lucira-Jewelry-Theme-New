{% schema %}
{
  "name": "Why Choose Section",
  "settings": [
    {
      "type": "text",
      "id": "header",
      "label": "Header",
      "default": "WHY CHOOSE LUCIRA JEWELRY?"
    },
    {
      "type": "text",
      "id": "subheader",
      "label": "Subheader",
      "default": "Lorem ipsum dolor sit amet, consectetur"
    },
    {
      "type": "text",
      "id": "gtm-prefix",
      "label": "GTM Prefix",
      "default": "why-choose-tile"
    }
  ],
  "blocks": [
    {
      "type": "feature_card",
      "name": "Feature Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "card_image",
          "label": "Card Background Image"
        },
        {
          "type": "image_picker",
          "id": "card_image_mobile",
          "label": "Card Background Image (Mobile)"
        },
        {
          "type": "url",
          "id": "card_link",
          "label": "Card Link"
        },
        {
          "type": "text",
          "id": "card_title",
          "label": "Card Title",
          "default": "Feature Title"
        },
        {
          "type": "text",
          "id": "card_subtitle",
          "label": "Card Subtitle",
          "default": "Feature subtitle"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Why Choose Section"
    }
  ],
  "max_blocks": 4
}
{% endschema %}

<section class="lucira-wc-section">
  <div class="lucira-wc-container">
    <h2 class="lucira-wc-header">{{ section.settings.header }}</h2>
    <p class="lucira-wc-subheader">{{ section.settings.subheader }}</p>

    <div class="lucira-wc-grid owl-carousel">
      {% for block in section.blocks %}
        <div class="lucira-wc-column" {{ block.shopify_attributes }}>
          <a 
            href="{{ block.settings.card_link }}" 
            class="lucira-wc-card {{ section.settings.gtm-prefix }}"
            data-info="tile-{{ forloop.index }}"
            data-desktop-img="{{ block.settings.card_image | img_url: '800x' }}"
            data-mobile-img="{{ block.settings.card_image_mobile | img_url: '800x' }}"
            style="background-repeat: no-repeat; background-size: cover; background-position: center; display: block;"
          >
            <div class="lucira-wc-card-content">
              <h3 class="lucira-wc-card-title">{{ block.settings.card_title }}</h3>
              <p class="lucira-wc-card-subtitle">{{ block.settings.card_subtitle }}</p>
            </div>
          </a>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    // Function to set appropriate background image based on screen size
    function setResponsiveImages() {
      const cards = document.querySelectorAll('.lucira-wc-card');
      const isMobile = window.innerWidth <= 768;
      
      cards.forEach(function(card) {
        const desktopImg = card.getAttribute('data-desktop-img');
        const mobileImg = card.getAttribute('data-mobile-img');
        
        // Use mobile image if available and screen is mobile, otherwise use desktop
        if (isMobile && mobileImg && mobileImg !== '') {
          card.style.backgroundImage = `url('${mobileImg}')`;
        } else if (desktopImg && desktopImg !== '') {
          card.style.backgroundImage = `url('${desktopImg}')`;
        }
      });
    }

    function initLuciraWCCarousel() {
      if (window.innerWidth <= 768) {
        if (!$('.lucira-wc-grid').hasClass('owl-loaded')) {
          $('.lucira-wc-grid').owlCarousel({
            loop: false,
            margin: 15,
            nav: false,
            dots: true,
            items: 1,
            autoplay: false,
            stagePadding: 30,
            center: false,
            responsive: {
              0: {
                items: 1,
                stagePadding: 30
              }
            }
          });
        }
      } else {
        if ($('.lucira-wc-grid').hasClass('owl-loaded')) {
          $('.lucira-wc-grid').trigger('destroy.owl.carousel');
          $('.lucira-wc-grid').removeClass('owl-carousel owl-loaded owl-drag');
        }
      }
    }

    // Set images on load
    setResponsiveImages();
    initLuciraWCCarousel();

    let resizeTimer;
    $(window).on('resize', function() {
      clearTimeout(resizeTimer);
      resizeTimer = setTimeout(function() {
        setResponsiveImages();
        initLuciraWCCarousel();
      }, 250);
    });

    // GTM Tracking
    const whyChooseTiles = document.querySelectorAll('.why-choose-tile');
    whyChooseTiles.forEach(function (tile) {
      tile.addEventListener('click', function () {
        const titleElement = tile.querySelector('.lucira-wc-card-title');
        const promoName = titleElement ? titleElement.textContent.trim() : 'Unknown Feature';
        const index = Array.from(whyChooseTiles).indexOf(tile) + 1;
        const promoId = `why-choose-${index}`;

        const promoData = {
          event: 'promoClick',
          promoClick: {
            promo_id: promoId,
            creative_name: 'why_choose_feature',
            promo_name: promoName,
          },
        };

        window.dataLayer = window.dataLayer || [];
        window.dataLayer.push(promoData);
      });
    });
  });
</script>

<style>
    .lucira-wc-section .owl-stage{padding-left:0!important}
    .lucira-wc-section{padding:30px 0}
    .lucira-wc-container{max-width:1400px;margin:0 auto;padding:0 20px}
    .lucira-wc-header{line-height:150%;font-size:24px;margin:0;font-weight:500;line-height:1.7;text-align:center;vertical-align:middle;text-transform:uppercase;color:#1a1a1a;margin-bottom:0;font-family:'Futura Std',sans-serif}
    .lucira-wc-subheader{text-align:center;font-size:18px;color:#666;font-family:'Futura Std',sans-serif;margin:0 0 40px 0}
    .lucira-wc-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:20px}
    .lucira-wc-column{position:relative;width:100%;overflow:hidden}
    .lucira-wc-card{display:block;width:100%;height:100%;background-size:cover;background-position:center;background-repeat:no-repeat;position:relative;overflow:hidden;transition:transform .3s ease;text-decoration:none}
    .lucira-wc-grid .lucira-wc-column:nth-child(1) .lucira-wc-card{border-radius:12px 12px 100px 12px;height:300px}
    .lucira-wc-grid .lucira-wc-column:nth-child(1) .lucira-wc-card-content{left:40px;top:40px}
    .lucira-wc-grid .lucira-wc-column:nth-child(1) .lucira-wc-card-subtitle{text-align:left}
    .lucira-wc-grid .lucira-wc-column:nth-child(2) .lucira-wc-card{border-radius:12px 12px 12px 100px;height:300px}
    .lucira-wc-grid .lucira-wc-column:nth-child(2) .lucira-wc-card-content{top:40px;right:40px}
    .lucira-wc-grid .lucira-wc-column:nth-child(2) .lucira-wc-card-subtitle{text-align:right}
    .lucira-wc-grid .lucira-wc-column:nth-child(2) .lucira-wc-card-title{text-align:right}
    .lucira-wc-grid .lucira-wc-column:nth-child(3) .lucira-wc-card{border-radius:12px 100px 12px 12px;height:300px}
    .lucira-wc-grid .lucira-wc-column:nth-child(3) .lucira-wc-card-content{bottom:40px;left:40px}
    .lucira-wc-grid .lucira-wc-column:nth-child(3) .lucira-wc-card-subtitle{text-align:left}
    .lucira-wc-grid .lucira-wc-column:nth-child(4) .lucira-wc-card{border-radius:100px 12px 12px 12px;height:300px}
    .lucira-wc-grid .lucira-wc-column:nth-child(4) .lucira-wc-card-content{bottom:40px;right:40px}
    .lucira-wc-grid .lucira-wc-column:nth-child(4) .lucira-wc-card-title{text-align:right}
    .lucira-wc-grid .lucira-wc-column:nth-child(4) .lucira-wc-card-subtitle{text-align:right}
    .lucira-wc-card-content{position:absolute;color:#fff;z-index:2}
    .lucira-wc-card-title{font-size:20px;font-weight:500;margin:0 0 8px 0;color:#fff;font-family:'Futura Std',sans-serif;text-transform:uppercase;letter-spacing:.08em;line-height:130%}
    .lucira-wc-card-subtitle{font-size:14px;margin:0;color:#fff;font-family:'Futura Std',sans-serif;opacity:.95}
    @media screen and (max-width:768px){
    .lucira-wc-section{padding:40px 0}
    .lucira-wc-header{font-size:18px}
    .lucira-wc-subheader{font-size:12px;margin-bottom:30px}
    .lucira-wc-grid{display:block;grid-template-columns:none;gap:0}
    .lucira-wc-column{width:100%;height:auto}
    .lucira-wc-card{border-radius:12px!important;height:212px!important;width:100%}
    .lucira-wc-grid .lucira-wc-column .lucira-wc-card-content{bottom:5px!important;left:10px!important;right:30px!important;top:auto!important}
    .lucira-wc-card-title{font-size:12px;margin-bottom:5px}
    .lucira-wc-card-subtitle{font-size:10px}
    .lucira-wc-grid.owl-carousel .owl-dots{text-align:center;margin-top:20px}
    .lucira-wc-grid.owl-carousel .owl-dot{display:inline-block;width:8px;height:8px;border-radius:50%;background:#ddd;margin:0 4px;border:none;padding:0;transition:all .3s ease}
    .lucira-wc-grid.owl-carousel .owl-dot.active{background:#1a1a1a;width:24px;border-radius:4px}
    }
    @media screen and (min-width:769px) and (max-width:1024px){
    .lucira-wc-header{font-size:22px}
    .lucira-wc-card-title{font-size:18px}
    .lucira-wc-card-subtitle{font-size:13px}
    }
    @media screen and (min-width:1600px){
    .lucira-wc-header{font-size:36px}
    .lucira-wc-subheader{font-size:20px;margin-bottom:60px}
    .lucira-wc-grid{gap:25px}
    .lucira-wc-card-title{font-size:32px}
    .lucira-wc-card-subtitle{font-size:18px}
    .lucira-wc-card-content{bottom:40px;left:40px;right:40px}
    }
</style>



{% comment %} 


{% schema %}
{
  "name": "Why Choose Section",
  "settings": [
    {
      "type": "text",
      "id": "header",
      "label": "Header",
      "default": "WHY CHOOSE LUCIRA JEWELRY?"
    },
    {
      "type": "text",
      "id": "subheader",
      "label": "Subheader",
      "default": "Lorem ipsum dolor sit amet, consectetur"
    },
    {
      "type": "text",
      "id": "gtm-prefix",
      "label": "GTM Prefix",
      "default": "why-choose-tile"
    }
  ],
  "blocks": [
    {
      "type": "feature_card",
      "name": "Feature Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "card_image",
          "label": "Card Background Image"
        },
        {
          "type": "image_picker",
          "id": "card_image_mobile",
          "label": "Card Background Image (Mobile)"
        },
        {
          "type": "url",
          "id": "card_link",
          "label": "Card Link"
        },
        {
          "type": "text",
          "id": "card_title",
          "label": "Card Title",
          "default": "Feature Title"
        },
        {
          "type": "text",
          "id": "card_subtitle",
          "label": "Card Subtitle",
          "default": "Feature subtitle"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Why Choose Section"
    }
  ],
  "max_blocks": 4
}
{% endschema %}

<section class="lucira-wc-section">
  <div class="page-width-wc-section">
  <div class="lucira-wc-container">
    <h2 class="lucira-wc-header">{{ section.settings.header }}</h2>
    <p class="lucira-wc-subheader">{{ section.settings.subheader }}</p>

    <div class="lucira-wc-grid owl-carousel">
      {% for block in section.blocks %}
        <div class="lucira-wc-column" {{ block.shopify_attributes }}>
          <a 
            href="{{ block.settings.card_link }}" 
            class="lucira-wc-card {{ section.settings.gtm-prefix }}"
            data-info="tile-{{ forloop.index }}"
            data-desktop-img="{{ block.settings.card_image | img_url: '800x' }}"
            data-mobile-img="{{ block.settings.card_image_mobile | img_url: '800x' }}"
            style="background-repeat: no-repeat; background-size: cover; background-position: center; display: block;"
          >
            <div class="lucira-wc-card-content">
              <h3 class="lucira-wc-card-title">{{ block.settings.card_title }}</h3>
              <p class="lucira-wc-card-subtitle">{{ block.settings.card_subtitle }}</p>
            </div>
          </a>
        </div>
      {% endfor %}
    </div>
  </div>
</div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    // Function to set appropriate background image based on screen size
    function setResponsiveImages() {
      const cards = document.querySelectorAll('.lucira-wc-card');
      const isMobile = window.innerWidth <= 768;
      
      cards.forEach(function(card) {
        const desktopImg = card.getAttribute('data-desktop-img');
        const mobileImg = card.getAttribute('data-mobile-img');
        
        // Use mobile image if available and screen is mobile, otherwise use desktop
        if (isMobile && mobileImg && mobileImg !== '') {
          card.style.backgroundImage = `url('${mobileImg}')`;
        } else if (desktopImg && desktopImg !== '') {
          card.style.backgroundImage = `url('${desktopImg}')`;
        }
      });
    }

    function initLuciraWCCarousel() {
      if (window.innerWidth <= 768) {
        if (!$('.lucira-wc-grid').hasClass('owl-loaded')) {
          $('.lucira-wc-grid').owlCarousel({
            loop: true,
            margin: 15,
            nav: false,
            dots: true,
            items: 1,
            autoplay: false,
            stagePadding: 30,
            center: false,
            responsive: {
              0: {
                items: 1,
                stagePadding: 30
              }
            }
          });
        }
      } else {
        if ($('.lucira-wc-grid').hasClass('owl-loaded')) {
          $('.lucira-wc-grid').trigger('destroy.owl.carousel');
          $('.lucira-wc-grid').removeClass('owl-carousel owl-loaded owl-drag');
        }
      }
    }

    // Set images on load
    setResponsiveImages();
    initLuciraWCCarousel();

    let resizeTimer;
    $(window).on('resize', function() {
      clearTimeout(resizeTimer);
      resizeTimer = setTimeout(function() {
        setResponsiveImages();
        initLuciraWCCarousel();
      }, 250);
    });

    // GTM Tracking
    const whyChooseTiles = document.querySelectorAll('.why-choose-tile');
    whyChooseTiles.forEach(function (tile) {
      tile.addEventListener('click', function () {
        const titleElement = tile.querySelector('.lucira-wc-card-title');
        const promoName = titleElement ? titleElement.textContent.trim() : 'Unknown Feature';
        const index = Array.from(whyChooseTiles).indexOf(tile) + 1;
        const promoId = `why-choose-${index}`;

        const promoData = {
          event: 'promoClick',
          promoClick: {
            promo_id: promoId,
            creative_name: 'why_choose_feature',
            promo_name: promoName,
          },
        };

        window.dataLayer = window.dataLayer || [];
        window.dataLayer.push(promoData);
      });
    });
  });
</script>

<style>



  .page-width-wc-section{
      max-width: 100% !important;
    width: 100% !important;
    margin: 0 auto !important;
    padding-left: 0 !important;
    padding-right: 0 !important;
  }
    .owl-stage{
    padding-left: 0px !important;
}
  /* Main Section Container */
  .lucira-wc-section {
    padding: 30px 0;
  }

  .lucira-wc-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 20px;
  }

  /* Headers */
  .lucira-wc-header {
    line-height: 150%;
    font-size: 24px;
    margin: 0;
    font-weight: 500;
    line-height: 1.7;
    text-align: center;
    vertical-align: middle;
    text-transform: uppercase;
    color: #1a1a1a;
    margin-bottom: 0;
    font-family: 'Futura Std', sans-serif;
  }

  .lucira-wc-subheader {
    text-align: center;
    font-size: 18px;
    color: #666;
    font-family: 'Futura Std', sans-serif;
    margin: 0 0 40px 0;
  }

  /* Grid Layout */
  .lucira-wc-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }

  .lucira-wc-column {
    position: relative;
    width: 100%;        
    overflow: hidden;
  }

  /* Card Wrapper */
  .lucira-wc-card {
    display: block;
    width: 100%;
    height: 100%;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    position: relative;
    overflow: hidden;
    transition: transform 0.3s ease;
    text-decoration: none;
  }

  /* Specific Card Styles */
  .lucira-wc-grid .lucira-wc-column:nth-child(1) .lucira-wc-card {
    border-radius: 12px 12px 100px 12px;
    height: 300px;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(1) .lucira-wc-card-content {    
    left: 35px;
    top: 35px;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(1) .lucira-wc-card-subtitle {    
    text-align: left;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(2) .lucira-wc-card {
    border-radius: 12px 12px 12px 100px;
    height: 300px;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(2) .lucira-wc-card-content {
    top: 35px;
    right: 35px;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(2) .lucira-wc-card-subtitle {    
    text-align: right;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(2) .lucira-wc-card-title {    
    text-align: right;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(3) .lucira-wc-card {
    border-radius: 12px 100px 12px 12px;
    height: 300px;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(3) .lucira-wc-card-content {
    bottom: 35px;
    left: 35px;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(3) .lucira-wc-card-subtitle {    
    text-align: left;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(4) .lucira-wc-card {
    border-radius: 100px 12px 12px 12px;
    height: 300px;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(4) .lucira-wc-card-content {
    bottom: 35px;
    right: 35px;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(4) .lucira-wc-card-title {    
    text-align: right;
  }

  .lucira-wc-grid .lucira-wc-column:nth-child(4) .lucira-wc-card-subtitle {    
    text-align: right;
  }

  /* Card Content */
  .lucira-wc-card-content {
    position: absolute;
    color: #fff;
    z-index: 2;
  }

  .lucira-wc-card-title {
    font-size: 20px;
    font-weight: 500;
    margin: 0 0 8px 0;
    color: #fff;
    font-family: 'Futura Std', sans-serif;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    line-height: 130%;
  }

  .lucira-wc-card-subtitle {
    font-size: 14px;
    margin: 0;
    color: #fff;
    font-family: 'Futura Std', sans-serif;
    opacity: 0.95;
  }

  /* Mobile Responsive - Carousel Styles */
  @media screen and (max-width: 768px) {
    .lucira-wc-section {
      padding: 40px 0;
    }

    .lucira-wc-header {
      font-size: 18px;
    }

    .lucira-wc-subheader {
      font-size: 12px;
      margin-bottom: 30px;
    }

    .lucira-wc-grid {
      display: block;
      grid-template-columns: none;
      gap: 0;
    }

    .lucira-wc-column {
      width: 100%;
      height: auto;
    }

    .lucira-wc-card {
      border-radius: 12px !important;
      height: 212px !important;
      width: 100%;
    }

    .lucira-wc-grid .lucira-wc-column .lucira-wc-card-content {
      bottom: 5px !important;
      left: 10px !important;
      right: 30px !important;
      top: auto !important;
    }

    .lucira-wc-card-title {
      font-size: 12px;
      margin-bottom: 5px;
    }

    .lucira-wc-card-subtitle {
      font-size: 10px;
    }

    /* Owl Carousel Dots - Custom styling for navigation */
    .lucira-wc-grid.owl-carousel .owl-dots {
      text-align: center;
      margin-top: 20px;
    }

    .lucira-wc-grid.owl-carousel .owl-dot {
      display: inline-block;
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: #ddd;
      margin: 0 4px;
      border: none;
      padding: 0;
      transition: all 0.3s ease;
    }

    .lucira-wc-grid.owl-carousel .owl-dot.active {
      background: #1a1a1a;
      width: 24px;
      border-radius: 4px;
    }
  }



  /* Tablet */
  @media screen and (min-width: 769px) and (max-width: 1024px) {
    .lucira-wc-header {
      font-size: 22px;
    }

    .lucira-wc-card-title {
      font-size: 18px;
    }

    .lucira-wc-card-subtitle {
      font-size: 13px;
    }
  }

  /* Large Desktop */
  @media screen and (min-width: 1600px) {
    .lucira-wc-header {
      font-size: 28px;
    }

      .lucira-wc-container {
    max-width: 1800px;
    margin: 0 auto;
    padding: 0 20px;
  }


    .lucira-wc-subheader {
      font-size: 18px;
      margin-bottom: 60px;
    }

    .lucira-wc-grid {
      gap: 28px;
    }

    .lucira-wc-card-title {
      font-size: 24px;
    }

    .lucira-wc-card-subtitle {
      font-size: 18px;
    }

    .lucira-wc-card-content {
      bottom: 40px;
      left: 40px;
      right: 40px;
    }
  }
</style> {% endcomment %}