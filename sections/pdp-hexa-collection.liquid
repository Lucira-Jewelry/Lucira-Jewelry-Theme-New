{% schema %}
{
  "name": "Hexa Cut Section",
  "settings": [
    {
      "type": "collection",
      "id": "target_collection",
      "label": "Target Collection",
      "info": "Select the collection where this section should appear on product pages"
    },
    {
      "type": "text",
      "id": "hexa_cut_subheading",
      "label": "Subheading",
      "default": "ON THE MOVE"
    },
    {
      "type": "text",
      "id": "hexa_cut_heading",
      "label": "Heading",
      "default": "for them who wanna stay in motion"
    },
    {
      "type": "richtext",
      "id": "hexa_cut_text",
      "label": "Text",
      "default": "<p>Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip consequat.</p>"
    }
  ],
  "presets": [
    {
      "name": "PDP Hexa Cut (Collection Products)"
    }
  ]
}
{% endschema %}

{%- assign target_collection = section.settings.target_collection -%}
{%- assign product_collections = product.collections -%}

{% comment %} Check if product belongs to target collection {% endcomment %}
{%- assign show_section = false -%}
{% for collection in product_collections %}
  {% if collection.handle == collections[target_collection].handle %}
    {% assign show_section = true %}
    {% break %}
  {% endif %}
{% endfor %}

{% if show_section %}
  <section class="hexa-cut-banner">
        <div class="hexa-cut-banner-wrapper">
            <div class="hexa-cut-content-wrap">
              {% if section.settings.hexa_cut_subheading != blank %}
                <h4>{{ section.settings.hexa_cut_subheading }}</h4>
              {% endif %}
              {% if section.settings.hexa_cut_heading != blank %}
                <h2>{{ section.settings.hexa_cut_heading }}</h2>
              {% endif %}
              {% if section.settings.hexa_cut_text != blank %}
                <div>{{ section.settings.hexa_cut_text }}</div>
              {% endif %}
            </div>
        </div>
  </section>
  <style>
  html {
    scroll-behavior: smooth;
  }
  .hexa-cut-banner-wrapper { 
    background: url(https://cdn.shopify.com/s/files/1/0739/8516/3482/files/Desktop_1.jpg?v=1756459478); 
    height: 710px; 
    background-size: 100%; 
    background-repeat: no-repeat; 
    padding: 70px 80px 70px; 
    position: relative;
    overflow: hidden;
  }

  .hexa-cut-content-wrap { 
    height: auto;
    position: absolute;
    top: 70px;
    left: 80px;
    right: 80px;
    transition: transform 0.1s ease-out;
  }
  .hexa-cut-banner-wrapper h4 { 
    font-family: Futura; 
    font-weight: 400; 
    font-size: 18px; 
    line-height: 20px; 
    text-align: left; 
    vertical-align: middle; 
    text-transform: uppercase; 
    margin-bottom: 14px;
    color: #FFFFFF; 
    margin-top: 0;
  }
  .hexa-cut-banner-wrapper h2 { 
    font-family: 'Futura'; 
    font-weight: 400; 
    font-size: 32px; 
    line-height: 20px; 
    text-transform: uppercase; 
    margin-top: 8px; 
    color: #FFFFFF; 
    text-align: left;
    margin-bottom: 36px;
  }
  .hexa-cut-banner-wrapper p { 
    font-family: 'Futura'; 
    text-align: left; 
    font-weight: 400; 
    font-size: 16px; 
    line-height: 24px; 
    vertical-align: middle; 
    color: #ffffff; 
    max-width: 620px; 
    margin-top: 20px; 
  }
  
  .hexa-cut-banner {
    padding: 0px 0 0 0;
  }
  .hexa-cut-banner-wrapper {position: relative; z-index: -1;}
  
  
  @media only screen and (min-width:1024px) and (max-width:1600px){
    .hexa-cut-banner-wrapper h2 { font-weight: 400; font-size: 32px; line-height: 1.4; text-transform: uppercase; margin-top: 10px; max-width: 535px; }
    .hexa-cut-banner-wrapper p {margin-top: 22px; max-width: 470px; }
    .hexa-cut-banner-wrapper {padding: 20px 80px 20px;}
    .hexa-cut-banner-wrapper {height: 570px;}
    .hexa-cut-content-wrap { top: 20px; left: 80px; right: 80px; }
    .hexa-cut-banner-wrapper h2 {    margin-bottom: 20px;}
    
  }

  @media only screen and (min-width:1500px) and (max-width:1679px){
    .hexa-cut-banner-wrapper {height: 575px;}
  }

  @media only screen and (min-width:1680px) and (max-width:1800px){
    .hexa-cut-banner-wrapper {height: 757px;}
  }

  @media only screen and (min-width:1680px) and (max-width:1800px){
    .hexa-cut-banner-wrapper {background-size: 100%;}
    .hexa-cut-banner-wrapper {height: 615px;}
  }

   @media only screen and (min-width:1801px) and (max-width:2000px){
     .hexa-cut-banner-wrapper h4 {margin-bottom: 30px;}
     .hexa-cut-banner-wrapper {height: 759px;}
     
   }
   @media only screen and (min-width:2001px) and (max-width:2600px){
    .hexa-cut-banner-wrapper { height: 762px; }
   }

  @media only screen and (min-width:1024px) and (max-width:1199px){
    .hexa-cut-banner-wrapper { height: 440px; }
  }

  @media only screen and (min-width:0px) and (max-width:767px){
    .hexa-cut-banner-wrapper p {font-family: 'Futura'; font-weight: 400; font-size: 18px; line-height: 1.6; margin-top: 6px;}
    .hexa-cut-banner-wrapper { background: url(https://cdn.shopify.com/s/files/1/0739/8516/3482/files/Mobile_1.jpg?v=1756459478); height: 490px; background-size: 100%; background-repeat: no-repeat; padding: 2px 22px 20px; }
    .hexa-cut-banner-wrapper h4 {margin-top: 0px; margin-bottom: 8px; color: #FFFFFF; font-family: 'Futura'; font-weight: 400; font-size: 12px; line-height: 20px; vertical-align: middle; text-transform: uppercase; }
    .hexa-cut-banner-wrapper h2 { color: #FFFFFF; margin: 0; margin-bottom: 8px; font-family: 'Futura'; font-weight: 400; font-size: 18px; line-height: 1.6; text-transform: uppercase; }
    .hexa-cut-banner-wrapper p { font-weight: 400; color: #ffffff; max-width: 620px; margin-top: 6px; font-family: 'Futura'; font-size: 12px; line-height: 16px; vertical-align: middle; }
    .hexa-cut-content-wrap { top: auto !important; bottom: 18px ; left: 22px; right: 22px; }
    .hexa-cut-content-wrap { top: 20px; transform: translateY(0) ! IMPORTANT; left: 22px; right: 22px; }
  }
  @media only screen and (min-width:376px) and (max-width:380px){
    .hexa-cut-banner-wrapper { height: 444px; }
  }

  @media only screen and (min-width:381px) and (max-width:390px){
   .hexa-cut-banner-wrapper { height: 476px; }
  }

  @media only screen and (min-width:376px) and (max-width:380px){
    .hexa-cut-banner-wrapper {height: 456px;}
  }

  @media only screen and (min-width:0px) and (max-width:375px){
    .hexa-cut-banner-wrapper {height: 463px;}
  }

  @media only screen and (min-width:361px) and (max-width:370px){
  .hexa-cut-banner-wrapper {height: 439px;}
  }

  @media only screen and (min-width:0px) and (max-width:360px){
   .hexa-cut-banner-wrapper { height: 440px; }
  }

  @media only screen and (min-width:0px) and (max-width:359px){
    .hexa-cut-banner-wrapper { height: 444px; }
  }
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Only run this effect on screens wider than 1024px
    if (window.innerWidth <= 1024) return;

    const section = document.querySelector('.hexa-cut-banner-wrapper');
    const content = document.querySelector('.hexa-cut-content-wrap');
    
    // Get initial measurements
    const contentHeight = content.offsetHeight;
    const sectionHeight = section.offsetHeight;
    const paddingTop = parseInt(window.getComputedStyle(section).paddingTop);
    const paddingBottom = parseInt(window.getComputedStyle(section).paddingBottom);
    
    // Calculate the available space for movement
    const availableSpace = sectionHeight - contentHeight - paddingTop - paddingBottom;
    
    // Only apply the effect if there's space to move (content is smaller than section)
    if (availableSpace > 0) {
        let lastScrollPosition = 0;
        let ticking = false;
        
        function updateContentPosition(scrollPos) {
            const sectionTop = section.offsetTop;
            const sectionBottom = sectionTop + sectionHeight;
            const viewportHeight = window.innerHeight;
            
            // Calculate how much of the section is visible (0 when above viewport, 1 when fully in view)
            const visibleRatio = Math.min(
                Math.max(
                    (scrollPos + viewportHeight - sectionTop) / (viewportHeight + sectionHeight),
                    0
                ),
                1
            );
            
            // Calculate the translateY position
            const translateY = visibleRatio * availableSpace;
            
            // Apply the transformation
            content.style.transform = `translateY(${translateY}px)`;
            content.style.willChange = 'transform';
        }
        
        window.addEventListener('scroll', function() {
            lastScrollPosition = window.scrollY;
            
            if (!ticking) {
                window.requestAnimationFrame(function() {
                    updateContentPosition(lastScrollPosition);
                    ticking = false;
                });
                ticking = true;
            }
        });
        
        // Initialize position
        updateContentPosition(window.scrollY);
    } else {
        // If content is taller than section, pin it to top
        content.style.transform = 'translateY(0)';
    }
});
</script>

{% endif %}