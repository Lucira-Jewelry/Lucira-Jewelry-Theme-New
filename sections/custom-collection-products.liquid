{% comment %}
  Featured Collection Section (Minimal) using card-product
{% endcomment %}

{%- assign skip_card_product_styles = false -%}

{{ 'plp-product-card.css' | asset_url | stylesheet_tag: preload: true }}
{{ 'product-card.css' | asset_url | stylesheet_tag: preload: true }}
{{ 'template-collection.css' | asset_url | stylesheet_tag: preload: true }}
<link
  rel="preload"
  href="{{ 'component-card.css' | asset_url }}"
  as="style"
  onload="this.onload=null;this.rel='stylesheet'"
>
<noscript><link rel="stylesheet" href="{{ 'component-card.css' | asset_url }}"></noscript>
{{ 'product-detail.css' | asset_url | stylesheet_tag: preload: true }}

{% if section.settings.image_shape == 'blob' %}
  {{ 'mask-blobs.css' | asset_url | stylesheet_tag: preload: true }}
{% endif %}

{% unless section.settings.quick_add == 'none' %}
  {{ 'quick-add.css' | asset_url | stylesheet_tag: preload: true }}
{% endunless %}

{% if section.settings.quick_add == 'standard' %}
  <script src="{{ 'quick-add.js' | asset_url }}" defer></script>
  <script src="{{ 'product-form.js' | asset_url }}" defer></script>
{% endif %}

{% if section.settings.quick_add == 'bulk' %}
  <script src="{{ 'quick-add-bulk.js' | asset_url }}" defer></script>
  <script src="{{ 'quantity-popover.js' | asset_url }}" defer></script>
  <script src="{{ 'price-per-item.js' | asset_url }}" defer></script>
  <script src="{{ 'quick-order-list.js' | asset_url }}" defer></script>
{% endif %}

<style>
  .custom-layout-section {
    display: flex;
    gap: 20px;
    align-items: start;
    margin: 0 auto;
  }

  .left-side {
    width: 70%;
  }
  .right-side {
    width: 30%;
    display: flex;
    flex-direction: column;
    gap: 20px;
    position: sticky;
    top: 0;
  }

  .facts_head {
    margin: 0;
    font-weight: 600;
  }

  /* --- FIXED: Show 2 lines before truncation, allow wrapping --- */
  .fact-text-wrapper {
    display: flex;
    align-items: flex-start;
    gap: 6px;
  }

  .fact-text {
    margin: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    line-height: 1.8em;
    display: block;
  }
    .chips_main {
        display: flex;
        gap: 12px;
        margin-bottom: 40px;
            margin-top: 10px;
        overflow: scroll;
    }
    .chips_capsule {
        border: 1px solid #1A1A1A;
        padding: 12px 32px;
        border-radius: 60px;
    }
  /* Read More Button */
  .read-more-btn {
    background: none;
    border: none;
    color: #000;
    padding: 0;
    cursor: pointer;
    text-decoration: underline;
    font-size: 14px;
  }

  .text-only-block,
  .collection-block {
    border: 1px solid #eee;
    border-radius: 8px;
    background: #fff;
  }

  .collection-products-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-bottom: 24px;
  }

  .view-all-wrapper {
    text-align: center;
    margin-bottom: 60
    px;
  }
  .view-all-btn {
    display: inline-block;
    padding: 17px 24px;
    background: #A68380;
    color: #fff;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 500;
    font-size: 12px;
    line-height: 100%;
    text-transform: uppercase;
  }
  .view-all-btn:hover {
    opacity: 0.9;
  }

  .facts_mob_main {
    display: none;
  }
    .text_only_block{
      background: #FBF3EC;
      padding: 30px;
      border-radius: 12px;
    }
  @media (max-width: 768px) {
    .custom-layout-section {
      flex-direction: column;
      gap: 12px;
    }
    .left-side {
      width: 100%;
    }
    .right-side {
      display: none;
    }

    .facts_mob_main {
      display: block;
      margin-bottom: 40px;
      padding: 20px;
      background-color: #fbf3ec;
    }

    .facts_head_mob {
      margin: 0;
      font-weight: 500;
      font-size: 20px;
      line-height: 150%;
      text-transform: uppercase;
    }

    .collection-products-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>

<div class="custom-layout-section custom_facts_collection_brad_{{ section.id }} page-width">
  <div class="left-side">
    <div class="chips_main">
      {% for block in section.blocks %}
        {% case block.type %}
          {% when 'chips_block' %}
            <div class="chips_capsule">{{ block.settings.chips_1 }}</div>
            <div class="chips_capsule">{{ block.settings.chips_2 }}</div>
            <div class="chips_capsule">{{ block.settings.chips_3 }}</div>
            <div class="chips_capsule">{{ block.settings.chips_4 }}</div>
            <div class="chips_capsule">{{ block.settings.chips_5 }}</div>
            <div class="chips_capsule">{{ block.settings.chips_6 }}</div>
            <div class="chips_capsule">{{ block.settings.chips_7 }}</div>
            <div class="chips_capsule">{{ block.settings.chips_8 }}</div>
            <div class="chips_capsule">{{ block.settings.chips_9 }}</div>
            <div class="chips_capsule">{{ block.settings.chips_10 }}</div>
        {% endcase %}
      {% endfor %}
    </div>
    {% for block in section.blocks %}
      {% case block.type %}
        {% when 'collection_block' %}
          <div class="collection-block" {{ block.shopify_attributes }}>
            {% if block.settings.collection %}
              <!-- MOBILE FACTS -->
              <div class="facts_mob_main">
                <p class="facts_head_mob">{{ block.settings.text_1 }}</p>

                <div class="fact-text-wrapper">
                  <p class="facts_content_mob fact-text" data-full-text="{{ block.settings.text_2 }}">
                    {{ block.settings.text_2 | truncate: 120 }}
                    <button class="read-more-btn" onclick="toggleReadMore(this)">Read More</button>
                  </p>
                </div>
              </div>

              {% assign custom_collection = block.settings.collection.products %}

              {% if custom_collection.size > 0 %}
                <div class="collection-products-grid">
                  {% for product in custom_collection limit: 4 %}
                    {% render 'card-product',
                      card_product: product,
                      media_aspect_ratio: section.settings.image_ratio,
                      image_shape: section.settings.image_shape,
                      show_secondary_image: section.settings.show_secondary_image,
                      show_vendor: section.settings.show_vendor,
                      show_rating: section.settings.show_rating,
                      skip_styles: skip_card_product_styles,
                      section_id: section.id,
                      quick_add: section.settings.quick_add
                    %}
                  {% endfor %}
                </div>

                <div class="view-all-wrapper">
                  <a href="{{ block.settings.collection.url }}" class="view-all-btn">
                    {{- block.settings.button_text -}}
                  </a>
                </div>

              {% else %}
                <p class="facts_content">No products found in this collection.</p>
              {% endif %}
            {% endif %}
          </div>
      {% endcase %}
    {% endfor %}
  </div>

  <!-- RIGHT SIDE FACTS BLOCKS -->
  <div class="right-side">
    {% for block in section.blocks %}
      {% case block.type %}
        {% when 'text_block' %}
          <div class="text-only-block_1 text_only_block" {{ block.shopify_attributes }}>
            <p class="facts_head">{{ block.settings.text_1 }}</p>

            <div class="fact-text-wrapper">
              <p class="facts_content fact-text" data-full-text="{{ block.settings.text_2 }}">
                {{ block.settings.text_2 | truncate: 120 }}
                <button class="read-more-btn" onclick="toggleReadMore(this)">Read More</button>
              </p>
            </div>
          </div>

          <div class="text-only-block_2 text_only_block" {{ block.shopify_attributes }}>
            <p class="facts_head">{{ block.settings.text_3 }}</p>

            <div class="fact-text-wrapper">
              <p class="facts_content fact-text" data-full-text="{{ block.settings.text_4 }}">
                {{ block.settings.text_4 | truncate: 120 }}
                <button class="read-more-btn" onclick="toggleReadMore(this)">Read More</button>
              </p>
            </div>
          </div>

          <div class="text-only-block_3 text_only_block" {{ block.shopify_attributes }}>
            <p class="facts_head">{{ block.settings.text_5 }}</p>

            <div class="fact-text-wrapper">
              <p class="facts_content fact-text" data-full-text="{{ block.settings.text_6 }}">
                {{ block.settings.text_6 | truncate: 120 }}
                <button class="read-more-btn" onclick="toggleReadMore(this)">Read More</button>
              </p>
            </div>
          </div>

          <div class="text-only-block_4 text_only_block" {{ block.shopify_attributes }}>
            <p class="facts_head">{{ block.settings.text_7 }}</p>

            <div class="fact-text-wrapper">
              <p class="facts_content fact-text" data-full-text="{{ block.settings.text_8 }}">
                {{ block.settings.text_8 | truncate: 120 }}
                <button class="read-more-btn" onclick="toggleReadMore(this)">Read More</button>
              </p>
            </div>
          </div>
      {% endcase %}
    {% endfor %}
  </div>
</div>

<script>
  function toggleReadMore(btn) {
    console.log('hahahha---------');
    const p = btn.previousElementSibling;
    const full = p.getAttribute('data-full-text');
    const truncated = full.length > 120 ? full.substring(0, 120) + '…' : full;

    if (p.classList.contains('expanded')) {
      p.classList.remove('expanded');
      p.style.maxHeight = '3.6em';
      p.textContent = truncated;
      btn.textContent = 'Read More';
    } else {
      p.classList.add('expanded');
      p.style.maxHeight = 'none';
      p.textContent = full;
      btn.textContent = 'Read Less';
    }
  }
  document.addEventListener('DOMContentLoaded', function () {
    const header = document.querySelector('header');
    const stickyElm = document.querySelector('.right-side');

    function updateTop() {
      if (!header || !stickyElm) return;
      stickyElm.style.top = header.offsetHeight + 'px';
    }

    updateTop();
    window.addEventListener('scroll', updateTop);
    window.addEventListener('resize', updateTop);
  });
</script>

{% schema %}
{
  "name": "Custom Collection",
  "settings": [
    {
      "type": "select",
      "id": "image_ratio",
      "label": "Image Aspect Ratio",
      "default": "square",
      "options": [
        { "label": "Square", "value": "square" },
        { "label": "Portrait", "value": "portrait" },
        { "label": "Landscape", "value": "landscape" }
      ]
    },
    {
      "type": "select",
      "id": "image_shape",
      "label": "Image Shape",
      "default": "default",
      "options": [
        { "label": "Default", "value": "default" },
        { "label": "Rounded", "value": "rounded" },
        { "label": "Blob", "value": "blob" }
      ]
    },
    {
      "type": "select",
      "id": "quick_add",
      "label": "Quick add type",
      "default": "none",
      "options": [
        { "label": "None", "value": "none" },
        { "label": "Standard", "value": "standard" },
        { "label": "Bulk", "value": "bulk" }
      ]
    },
    { "type": "checkbox", "id": "show_secondary_image", "label": "Show secondary image", "default": false },
    { "type": "checkbox", "id": "show_vendor", "label": "Show vendor", "default": false },
    { "type": "checkbox", "id": "show_rating", "label": "Show rating", "default": false }
  ],

  "blocks": [
    {
      "type": "collection_block",
      "name": "Collection Block",
      "settings": [
        { "type": "collection", "id": "collection", "label": "Select Collection" },
        { "type": "text", "id": "chips_text", "label": "Relatable Chips Name" },
        { "type": "text", "id": "button_text", "label": "Button Text" },
        { "type": "text", "id": "text_1", "label": "Facts Heading (Mobile)" },
        { "type": "text", "id": "text_2", "label": "Facts Content (Mobile)" }
      ]
    },

    {
      "type": "text_block",
      "name": "Text Only Block",
      "limit": 1,
      "settings": [
        { "type": "text", "id": "text_1", "label": "Facts Heading" },
        { "type": "text", "id": "text_2", "label": "Facts Content" },
        { "type": "text", "id": "text_3", "label": "Facts Heading" },
        { "type": "text", "id": "text_4", "label": "Facts Content" },
        { "type": "text", "id": "text_5", "label": "Facts Heading" },
        { "type": "text", "id": "text_6", "label": "Facts Content" },
        { "type": "text", "id": "text_7", "label": "Facts Heading" },
        { "type": "text", "id": "text_8", "label": "Facts Content" }
      ]
    },
    {
  "type": "chips_block",
  "name": "Chips",
  "limit": 1,
  "settings": [
    { "type": "text", "id": "chips_1", "label": "Chips Heading 1" },
    { "type": "text", "id": "chips_2", "label": "Chips Heading 2" },
    { "type": "text", "id": "chips_3", "label": "Chips Heading 3" },
    { "type": "text", "id": "chips_4", "label": "Chips Heading 4" },
    { "type": "text", "id": "chips_5", "label": "Chips Heading 5" },
    { "type": "text", "id": "chips_6", "label": "Chips Heading 6" },
    { "type": "text", "id": "chips_7", "label": "Chips Heading 7" },
    { "type": "text", "id": "chips_8", "label": "Chips Heading 8" },
    { "type": "text", "id": "chips_9", "label": "Chips Heading 9" },
    { "type": "text", "id": "chips_10", "label": "Chips Heading 9" }
  ]
}
  ],

  "presets": [{ "name": "Custom Collection" }]
}
{% endschema %}
