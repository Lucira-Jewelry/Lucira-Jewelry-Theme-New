{% comment %}
  Renders product variant options with dynamic stock update

  Accepts:
  - product: {Object} product object.
  - option: {Object} current product_option object.
  - block: {Object} block object.
  - picker_type: {String} type of picker to dispay
{% endcomment %}

{%- liquid
  assign product_form_id = 'product-form-' | append: section.id
-%}

{%- if option.name == 'Size' -%}
  {%- assign sorted_values = option.values | sort -%}
{%- else -%}
  {%- assign sorted_values = option.values -%}
{%- endif -%}

{%- for value in sorted_values -%}
  {%- liquid
    assign swatch_focal_point = null
    if value.swatch.image
      assign image_url = value.swatch.image | image_url: width: 50
      assign swatch_value = 'url(' | append: image_url | append: ')'
      assign swatch_focal_point = value.swatch.image.presentation.focal_point
    elsif value.swatch.color
      assign swatch_value = 'rgb(' | append: value.swatch.color.rgb | append: ')'
    else
      assign swatch_value = null
    endif

    assign option_disabled = true
    if value.available
      assign option_disabled = false
    endif
  -%}

  {%- capture input_id -%}
    {{ section.id }}-{{ option.position }}-{{ forloop.index0 -}}
  {%- endcapture -%}

  {%- capture input_name -%}
    {{ option.name }}-{{ option.position }}
  {%- endcapture -%}

  {%- capture input_dataset -%}
    data-product-url="{{ value.product_url }}"
    data-option-value-id="{{ value.id }}"
  {%- endcapture -%}

  {%- capture label_unavailable -%}
    <span class="visually-hidden label-unavailable">
      {{- 'products.product.variant_sold_out_or_unavailable' | t -}}
    </span>
  {%- endcapture -%}

  {%- assign is_default_selected = false -%}
  {%- if value == '12' and option.selected_value == blank -%}
    {%- assign is_default_selected = true -%}
  {%- endif -%}

  {%- if picker_type == 'swatch' -%}
    {%- capture help_text -%}
      <span class="visually-hidden">{{ value | escape }}</span>
      {{ label_unavailable }}
    {%- endcapture -%}
    {%
      render 'swatch-input',
      id: input_id,
      name: input_name,
      value: value | escape,
      swatch: value.swatch,
      product_form_id: product_form_id,
      checked: value.selected or is_default_selected,
      visually_disabled: option_disabled,
      shape: block.settings.swatch_shape,
      help_text: help_text,
      additional_props: input_dataset
    %}
  {%- elsif picker_type == 'button' -%}
    <input
      type="radio"
      id="{{ input_id }}"
      name="{{ input_name | escape }}"
      value="{{ value | escape }}"
      form="{{ product_form_id }}"
      {% if value.selected %} checked {% endif %}
      {% if option_disabled %} class="disabled" {% endif %}
      {{ input_dataset }}
    >

    <label for="{{ input_id }}" class="variant-label">
      {%- assign v = value | downcase -%}

      {%- if v contains 'yellow' or v contains 'rose' or v contains 'white' or v contains 'silver' -%}
        <span class="variant-swatch" aria-hidden="true">
          {%- if v contains 'yellow' -%}
            {{- 'yellow-color.svg' | inline_asset_content -}}
          {%- elsif v contains 'rose' or v contains 'rose gold' -%}
            {{- 'rose-color.svg' | inline_asset_content -}}
          {%- elsif v contains 'white' or v contains 'silver' or v contains 'white gold' -%}
            {{- 'silver-color.svg' | inline_asset_content -}}
          {%- endif -%}
        </span>
      {%- endif -%}

      <span class="variant-text" style="display: inline-block !important;">
        {{ value }}
        <span class="Variant_Stock_Status">Loading...</span>
        {{ label_unavailable }}
      </span>
    </label>

  {%- elsif picker_type == 'dropdown' or picker_type == 'swatch_dropdown' -%}
    <option
      id="{{ input_id }}"
      value="{{ value | escape }}"
      {% if value.selected %} selected="selected" {% endif %}
      {% if swatch_value and picker_type == 'swatch_dropdown' %}
        data-option-swatch-value="{{ swatch_value }}"
        {% if swatch_focal_point %}
          data-option-swatch-focal-point="{{ swatch_focal_point }}"
        {% endif %}
      {% endif %}
      {{ input_dataset }}
    >
      {% if option_disabled -%}
        {{- 'products.product.value_unavailable' | t: option_value: value -}}
      {%- else -%}
        {{- value -}}
      {%- endif %}
    </option>
  {%- endif -%}
{%- endfor -%}

{% comment %}
  Pass product variants to JS for dynamic stock update
{% endcomment %}
<script>
window.productVariants = {{ product.variants | json }};

document.addEventListener('DOMContentLoaded', function() {
  function updateStockStatus() {
    // Get selected color
    const selectedColor = document.querySelector('[name="Color-1"]:checked')?.value || '';
    const sizeInputs = document.querySelectorAll('[name="Size-2"]'); // adjust to your input_name
    const stockLabels = document.querySelectorAll('.Variant_Stock_Status');

    sizeInputs.forEach((radio, index) => {
      const size = radio.value;
      let variantAvailable = false;

      window.productVariants.forEach(v => {
        if (v.option1 === selectedColor && v.option2 === size && v.available && v.inventory_quantity > 0) {
          variantAvailable = true;
        }
      });

      const label = stockLabels[index];
      if (label) {
        label.innerHTML = variantAvailable 
          ? '<span class="stock-active">In Stock</span>'
          : 'Make to Order';
      }
    });
  }

  // Listen to changes
  document.querySelectorAll('[name="Size-2"]').forEach(input => input.addEventListener('change', updateStockStatus));
  document.querySelectorAll('[name="Color-1"]').forEach(input => input.addEventListener('change', updateStockStatus));

  // Initial run
  updateStockStatus();
});
</script>
